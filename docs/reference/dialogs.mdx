---
sidebar_position: 10
sidebar_label: 'Dialogs'
title: 'Dialogs'
description: 'The dialogs PSAppDeployToolkit can display, and the commands that show them.'
slug: /usage/adding-ui-elements
hide_title: true
tags:
  - User Interface
  - Reference
---

import ThemedImage from '@theme/ThemedImage';

## Dialogs

The dialogs below are shown by the commands listed against each one. All of them:

- Take their text from `strings.psd1`, localized into 25 languages, selected from the operating
  system unless `UI.LanguageOverride` is set. See [Language Strings](./language-strings.mdx).
- Follow the light or dark theme of the user's session. (the screenshots in this documentation
  will follow the current desktop theme. You can change between light / dark by adjusting the 
  selected theme in the top right hand corner of this page).
- Are branded from the `Assets` folder, using a logo for the Fluent dialogs and a banner for the
  Classic dialogs.
- Are suppressed according to the deployment mode. `-DeployMode Silent` shows none of them; see
  [Deployment Modes](../explanation/deployment-modes.mdx).

The screenshots show the Fluent dialogs, which is the default for any new deployments. The 
Classic dialogs are shown when `UI.DialogStyle` is `Classic`, and are the only set available in 
v3 compatibility mode.

## Installation progress

Displays a progress bar and status text. Shown by
[`Show-ADTInstallationProgress`](./functions/Show-ADTInstallationProgress.mdx); closed by
[`Close-ADTInstallationProgress`](./functions/Close-ADTInstallationProgress.mdx).

The bar is indeterminate by default, which is the right choice when your script cannot tell how far
along it is.

<ThemedImage
  alt="The installation progress dialog with an indeterminate progress bar"
  className="markdown_image"
  sources={{
    light: require('../images/progress_plain_light.png').default,
    dark: require('../images/progress_plain_dark.png').default,
  }}
/>

Calling the command again while the dialog is open updates it in place rather than opening a second
dialog, which is how a long install is narrated. Supplying `-StatusBarPercentage` switches the bar
to show a specific percentage, and `-StatusMessageDetail` adds a second line beneath the message.

<ThemedImage
  alt="The progress dialog showing a detail line and the bar at 75 percent"
  className="markdown_image"
  sources={{
    light: require('../images/progress_detail_75_light.png').default,
    dark: require('../images/progress_detail_75_dark.png').default,
  }}
/>

## Installation welcome

Lists applications that must be closed, and optionally offers deferral and a countdown. Shown by
[`Show-ADTInstallationWelcome`](./functions/Show-ADTInstallationWelcome.mdx).

With applications to close and deferral offered, the dialog lists each application and shows how
many deferrals are left.

<ThemedImage
  alt="The welcome dialog listing three applications to close, with a Defer button and three deferrals remaining"
  className="markdown_image"
  sources={{
    light: require('../images/welcome_fluent_light.png').default,
    dark: require('../images/welcome_fluent_dark.png').default,
  }}
/>

With `-AllowDefer` but nothing running to close, the dialog asks only when to start.

<ThemedImage
  alt="The welcome dialog offering Install and Defer with no applications listed"
  className="markdown_image"
  sources={{
    light: require('../images/welcome_defer_only_light.png').default,
    dark: require('../images/welcome_defer_only_dark.png').default,
  }}
/>

`-CloseProcessesCountdown` adds a countdown, after which the listed applications are closed and the
deployment continues.

<ThemedImage
  alt="The welcome dialog counting down before it closes the listed applications"
  className="markdown_image"
  sources={{
    light: require('../images/welcome_countdown_light.png').default,
    dark: require('../images/welcome_countdown_dark.png').default,
  }}
/>

The limits combine. Here `-DeferDeadline` fixes the date after which deferral is no longer offered,
while `-ForceCountdown` starts the installation automatically when its timer expires, so the dialog
shows both.

<ThemedImage
  alt="The welcome dialog showing an automatic start countdown and a deferral deadline together"
  className="markdown_image"
  sources={{
    light: require('../images/welcome_deadline_force_light.png').default,
    dark: require('../images/welcome_deadline_force_dark.png').default,
  }}
/>

Usage is covered in
[How to close applications and allow deferral](../how-to/close-apps-and-allow-deferral.mdx).

## Installation prompt

A branded prompt with a custom title, message and buttons. Shown by
[`Show-ADTInstallationPrompt`](./functions/Show-ADTInstallationPrompt.mdx). Message text supports
[text formatting](./text-formatting.mdx).

With one button it reports a result and waits for acknowledgement.

<ThemedImage
  alt="An informational prompt with a single OK button"
  className="markdown_image"
  sources={{
    light: require('../images/prompt_info_light.png').default,
    dark: require('../images/prompt_info_dark.png').default,
  }}
/>

`-ButtonLeftText` and `-ButtonRightText` give two choices.

<ThemedImage
  alt="A prompt offering two buttons, Remind me later and Install now"
  className="markdown_image"
  sources={{
    light: require('../images/prompt_two_button_light.png').default,
    dark: require('../images/prompt_two_button_dark.png').default,
  }}
/>

Adding `-ButtonMiddleText` gives three.

<ThemedImage
  alt="A prompt offering three buttons, Uninstall, Repair and Cancel"
  className="markdown_image"
  sources={{
    light: require('../images/prompt_three_button_light.png').default,
    dark: require('../images/prompt_three_button_dark.png').default,
  }}
/>

`-RequestInput` collects a value from the user.

<ThemedImage
  alt="A prompt with an empty text field for the user to complete"
  className="markdown_image"
  sources={{
    light: require('../images/prompt_input_light.png').default,
    dark: require('../images/prompt_input_dark.png').default,
  }}
/>

`-DefaultValue` pre-fills the field, which turns the question into a confirmation.

<ThemedImage
  alt="The same input prompt pre-filled with an asset tag"
  className="markdown_image"
  sources={{
    light: require('../images/prompt_input_default_light.png').default,
    dark: require('../images/prompt_input_default_dark.png').default,
  }}
/>

`-SecureInput` masks what is typed, for licence keys and similar. The field looks the same until
the user types into it.

<ThemedImage
  alt="An input prompt whose field masks what the user types"
  className="markdown_image"
  sources={{
    light: require('../images/prompt_input_secure_light.png').default,
    dark: require('../images/prompt_input_secure_dark.png').default,
  }}
/>

`-ListItems` presents a list to choose from, with `-DefaultIndex` selecting the initial entry. One
item is chosen, not several.

<ThemedImage
  alt="A prompt asking the user to choose one item from a list"
  className="markdown_image"
  sources={{
    light: require('../images/prompt_list_light.png').default,
    dark: require('../images/prompt_list_dark.png').default,
  }}
/>

:::note
`-Icon` is accepted but has no effect on the Fluent dialogs. It applies to the Classic dialogs only.
:::

## Installation restart prompt

Asks the user to restart. Shown by
[`Show-ADTInstallationRestartPrompt`](./functions/Show-ADTInstallationRestartPrompt.mdx). The prompt
runs in a separate PowerShell session, so the deployment still returns its exit code to the parent
process.

`-NoCountdown` asks without imposing a deadline. Nothing happens until the user chooses.

<ThemedImage
  alt="The restart prompt with Restart Now and Minimize buttons and no countdown"
  className="markdown_image"
  sources={{
    light: require('../images/restart_nocountdown_light.png').default,
    dark: require('../images/restart_nocountdown_dark.png').default,
  }}
/>

`-Countdown` restarts the computer automatically when the timer reaches zero.

<ThemedImage
  alt="The restart prompt showing the time remaining before an automatic restart"
  className="markdown_image"
  sources={{
    light: require('../images/restart_countdown_light.png').default,
    dark: require('../images/restart_countdown_dark.png').default,
  }}
/>

`-CountdownNoHide` runs the same countdown but takes away the option to minimise it.

<ThemedImage
  alt="The restart prompt counting down with no Minimize button"
  className="markdown_image"
  sources={{
    light: require('../images/restart_countdown_nohide_light.png').default,
    dark: require('../images/restart_countdown_nohide_dark.png').default,
  }}
/>

## Dialog box

A generic, unbranded message box with configurable system icon and buttons. Shown by
[`Show-ADTDialogBox`](./functions/Show-ADTDialogBox.mdx). Because it is a Windows message box rather
than a toolkit dialog, it takes no branding and ignores the dialog style.

<ThemedImage
  alt="An unbranded message box with OK and Cancel buttons"
  className="markdown_image"
  sources={{
    light: require('../images/dialogbox_okcancel_light.png').default,
    dark: require('../images/dialogbox_okcancel_dark.png').default,
  }}
/>

`-Buttons` selects the combination, from `Ok` through to `CancelTryContinue`.

<ThemedImage
  alt="An unbranded message box with Yes, No and Cancel buttons"
  className="markdown_image"
  sources={{
    light: require('../images/dialogbox_yesnocancel_light.png').default,
    dark: require('../images/dialogbox_yesnocancel_dark.png').default,
  }}
/>

## Balloon tips and notifications

Transient notifications are shown by
[`Show-ADTBalloonTip`](./functions/Show-ADTBalloonTip.mdx), and the notification area icon by
[`Show-ADTNotifyIcon`](./functions/Show-ADTNotifyIcon.mdx) and
[`Close-ADTNotifyIcon`](./functions/Close-ADTNotifyIcon.mdx). Balloon notifications are governed by
the `UI.BalloonNotifications` setting, and are delivered by Windows rather than drawn by the
toolkit, so they follow whatever notification settings apply to the device.

## Help console

A browsable list of the toolkit's commands and their help, shown by
[`Show-ADTHelpConsole`](./functions/Show-ADTHelpConsole.mdx). Intended for use while authoring a
package, not during a deployment.

<img src={require('../images/helpconsole_42.png').default} alt="The help console listing the toolkit's commands with the help for the selected one" class="markdown_image" />
